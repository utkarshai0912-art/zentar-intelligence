import type { Metadata } from "next";
import { Inter, JetBrains_Mono } from "next/font/google";
import "./globals.css";
import { Providers } from "@/components/layout/Providers";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

const jetbrainsMono = JetBrains_Mono({
  subsets: ["latin"],
  variable: "--font-jetbrains-mono",
});

export const metadata: Metadata = {
  title: "Zentar Intelligence — AI Tools Hub",
  description:
    "A modern modular AI Tools Hub with specialized AI assistants for thumbnails, scripts, ads, messaging, and more.",
  keywords: "AI tools, content creation, thumbnail analyzer, script writer, AI assistant",
  openGraph: {
    title: "Zentar Intelligence",
    description: "AI Tools Hub for Content Creators",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${inter.variable} ${jetbrainsMono.variable} font-sans antialiased`}>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
